// Module syntax (latest)

import { getPosts, getPostsLength } from './postController.js';

console.log(getPosts(), getPostsLength());


// common js syntax

// const {generateRandomNumbers, celiusToFahrenheit} = require('./utils');

// const randomNo = generateRandomNumbers();

// console.log(`Random no ${randomNo}`);

// console.log(`Temperature in Celius ${celiusToFahrenheit(randomNo)}`);


